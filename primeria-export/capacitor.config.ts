import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId:   "com.primeria.demo",
  appName: "Primeria",
  webDir:  "dist",
  android: {
    backgroundColor: "#000000",
  },
};

export default config;
